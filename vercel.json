{
    "installCommand": "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python"
        }
    ]
}